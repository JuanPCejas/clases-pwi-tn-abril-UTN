**Si empezamos dessde 0:

1- git init
2- git add .
3- git commit -m 'comentario X'
4- git branch -M main
5- git remote add origin y copiar la direccion que nos da el repositorio previamente creado en github
6- git push -u origin main

Listo con eso tenemos todo en git hub.


**Para actualizar solamente:

1- git add .
2- git commit -m 'comentario X'
3- git push


**Si me equivoque al poner el git remote add origin usar el siguiente comando:

1- git remote set-url <URL_CORRECTA>

**Para ver la informacion detallada de nuestro repo podemos usar el comando:

1- git status

**Para ver que origen remoto tengo utilizar el siguiente codigo:

1- git remote -v Fetch: donde recibo Push: donde envio

**Para clonar un repositorio:
1- voy a github y selecciono el commit a clonar
2- Le doy copiar, desde los dos cuadraditos
3- Genero una nueva carpeta en el escritorio o donde quiera guardarlo
4- Click dderecho y abrir con code 
5- desde terminal ingreso git clone y pego lo copiado